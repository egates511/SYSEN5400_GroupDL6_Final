# SYSEN5400_GroupDL6_Final
Files with code used to generate calculations, tables, and graphs used in the final report section 3. 

To use the R programs, ensure you have the following packages installed on R:
Note, this does include the archr package, so the archr-main zip needs to be in the owrking directory.

install.packages(c("dplyr","readr","ggplot2","GA","ggraph","gtools","rmoo","gally"))
install.packages("archr_1.0.tar.gz",type="source")
